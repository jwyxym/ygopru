# 工程
这个工程是将../ygopro移植到rust的工程。
当前的核心是移植../ygopro的gframe/single_duel
ygopro-data和ygopro-handler已经抽象了结构。
现在，./ygopro的目标是将其作为一个Provider使用。

# 编程偏好
## 命名
- **禁止缩写** — 变量名必须完整拼写，如 `client_to_server_sender` 而非 `tx`。
- **方向命名** — 数据发往某个远端叫 `client_to_server`，反方向叫 `server_to_client`。
- **模板参数** — 必须用有意义的名称（`ClientToServerStream` 而非 `StreamType`）。

## import 风格
- 每个 `use` 独占一行，禁止 `use a::{b, c}`。
- 按 crate 分组，标准库在前，外部 crate 次之，内部 crate 最后。

## 注释
- 只写"为什么这么做"，不写"在做什么"。代码自解释。
- 模块级 `//!` 和条目级 `///` 文档注释用英文。

## 格式
- **函数签名** — 无 `where` 子句时或者只有1个where约束时必须写在一行，有两个以上的 `where` 才能换行。严禁考虑任何单行长度限制，长度限制是用户手动考虑的事情，换行是丑陋的。
- **忽略 Result** — 用 `.ok()` 而非 `let _ =`。

## 沟通
- 中文对话，英文文档。
- 先概述方案再动手，不要擅自改。
- 复杂的架构设计需要反复推敲方向和意图。

## 禁则
- 严禁使用git恢复数据。
- 除非用户明确允许你写代码，否则不要碰任何代码，用户的明确允许只生效1次，在对话历史中允许你修改代码不代表本次允许你修改代码。
- 用户随时可能修改代码。每次你动手修改代码前，你必须重新读取文件，严禁直接使用记忆中的文件进行修改。

## 你容易烦的错误
- 无论是C++还是Rust，回放（.yrp）文件**不含** game message 流，只包含 Response 消息
- 你的目标是严格按照用户的要求写出简洁、可用、人类可以维护的代码，不是可以编译的狗屎！！！
